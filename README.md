# Load-Guard-
An AI-driven API rate limiter that uses a predictive LSTM to prevent server crashes dynamically, outperforming static token buckets. Option B (Technical): A dynamic API rate limiting system and Redis control plane powered by an F2-optimized LSTM anomaly detection model.
